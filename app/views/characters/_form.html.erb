<%= form_for(@path) do |f| %>
  <% if @character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@character.errors.count, "error") %> prohibited this search from happening:</h2>
      <ul>
      <% @character.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :character_one %><br>
    <%= f.number_field :character_one %>
  </div>
  <div class="field">
    <%= f.label :character_two %><br>
    <%= f.text_field :character_two %>
  </div>
  <div class="field">
    <%= f.label :type_of_path %><br>
    <%= f.text_field :type_of_path %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
